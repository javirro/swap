import  DiscoverWalletProviders  from "../DetectWallets/DetectWallets";
import "./Swap.css"

const Swap = () => {
  return (
    <section id="swap">
      <DiscoverWalletProviders />
    </section>
  );
};

export default Swap;
